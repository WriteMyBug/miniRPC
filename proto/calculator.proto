syntax = "proto3";

package minirpc.example;

option cc_generic_services = true;

message CalcRequest {
  double a = 1;
  double b = 2;
  string op = 3;  // "+" "-" "*" "/"
}

message CalcResponse {
  double result = 1;
  string error = 2;
  string server_id = 3;  // 处理该请求的节点标识（演示/压测用）
}

service CalculatorService {
  rpc Calc(CalcRequest) returns (CalcResponse);
}
