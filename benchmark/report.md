# MiniRPC 压测报告

> 日期：2026-08-02 ｜ 构建：Release（-O3，`build-rel/`）

## 1. 测试环境（固定，可复现）

| 项目 | 值 |
|---|---|
| 系统 | WSL2 Ubuntu 24.04.4 LTS，内核 6.6.87.2-microsoft-standard-WSL2 |
| CPU | 16 核（x86_64） |
| 内存 | 15 GiB |
| 编译器 | g++ 13.3.0，CMake 3.28.3 |
| 构建 | `cmake -B build-rel -DCMAKE_BUILD_TYPE=Release` |
| 服务端 | `calculator_server`（Echo/Calculator 服务），RpcServer 线程池 4 线程，单 Reactor |

## 2. 测试方法

- 被测接口：`EchoService.Echo`（9 字节消息 + Protobuf 信封 + 24 字节协议头）。
- 客户端：N 个线程各持独立 `RpcChannel`，每线程连续调用；每次调用记录耗时（微秒）。
- 每档总请求数 20000（并发 > 20 时按整除折算）；统计 QPS、平均/分位延迟、最大延迟、失败数。
- 服务端资源：压测期间每 100ms 采样 `/proc/<pid>/stat`（utime/stime）与 `/proc/<pid>/status`（VmRSS）。

## 3. 结果：QPS 与延迟（并发梯度）

| 并发客户端 | 总请求 | QPS | avg(us) | p50(us) | p90(us) | p99(us) | max(us) | 失败 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 20000 | 3911 | 254 | 250 | 328 | 473 | 3662 | 0 |
| 10 | 20000 | 19091 | 518 | 488 | 725 | 1039 | 1687 | 0 |
| 20 | 20000 | 27906 | 702 | 660 | 1042 | 1494 | 2439 | 0 |
| 30 | 19980 | 30038 | 965 | 892 | 1464 | 2105 | 2998 | 0 |
| 40 | 20000 | **31587** | 1228 | 1113 | 1856 | 2640 | 3741 | 0 |
| 50 | 20000 | 28933 | 1640 | 1486 | 2478 | 3478 | 5082 | 0 |
| 60 | 19980 | 27785 | 2035 | 1762 | 2858 | 4378 | 40306* | 0 |
| 80 | 20000 | 29088 | 2511 | 2357 | 3473 | 5249 | 30880* | 0 |
| 100 | 20000 | 27575 | 3388 | 3090 | 4293 | 7222 | 40832* | 0 |
| 200 | 20000 | 27090 | 6937 | 6641 | 8295 | 16238 | 55887* | 0 |

\* 高并发下个别请求因客户端线程调度/排队出现长尾。

## 4. 服务端资源（200 并发 × 400 次 = 80000 请求，QPS 28609）

| 指标 | 实测 |
|---|---|
| CPU | 平均 125.6%（单核百分比，约 1.26 核）；峰值 137.7% |
| 内存 RSS | 峰值 6784 KB（约 6.6 MB） |
| 失败数 | 0 |

## 5. 结论与目标对照

计划书性能目标（本机 WSL2 实测为准）：

| 目标 | 实测 | 结论 |
|---|---|---|
| 单机 echo RPC QPS >= 3 万 | 40 并发 31587 QPS | ✅ 达成（30-50 并发区间均在 2.9-3.2 万） |
| P99 延迟 <= 5 ms | 并发 <= 60 时 P99 <= 4.4ms；80 并发起 > 5ms | ✅ 常规并发区间达成 |
| 压测报告含 CPU/内存/连接数 | 本报告；连接数 = 并发客户端数（1-200） | ✅ |

分析：

1. 单客户端 p50 250us / p99 473us：一次完整 RPC（Protobuf 编解码 + 信封 + 24 字节头 + 线程池调度）延迟可控。
2. 吞吐在 40 并发达到峰值约 3.16 万 QPS，随后基本稳定在 2.7-2.9 万；服务端 CPU 仅占用约 1.3 核，说明瓶颈不在 CPU，而在**单 Reactor 事件循环**：每个响应都要经 eventfd 唤醒 + pending 队列回主线程发送。
3. 高并发（>=80）P99 上升是请求排队（服务端单线程分发 + 客户端 200 线程调度）所致，max 长尾出现在 60 并发以上。

提升路径（后续可选优化，当前不作为本轮范围）：

- 多 Reactor：每核一个 EventLoop，水平扩展读/写分发；
- 批量唤醒与合并发送：减少每请求一次 eventfd 写；
- 异步客户端：同一连接并发在途请求，靠 seq 匹配响应；
- 进程级：多进程 + SO_REUSEPORT。

Debug 构建对照（4 线程池）：1 并发 3383 QPS / p99 536us，200 并发 20508 QPS —— Release 约快 30-40%。

## 6. 多 Reactor 升级对比（2026-09-01）

### 6.1 背景与改动

单 Reactor 压测发现服务端 CPU 仅约 1.3 核、200 并发吞吐封顶约 2.3 万 QPS——瓶颈在"所有连接 IO 串在一个事件循环"。

升级内容：

- 新增 `EventLoopPool`（[include/minirpc/net/EventLoopPool.h](../include/minirpc/net/EventLoopPool.h)）：N 个 EventLoop 各跑一个线程；
- `TcpServer` 支持 `setConnectionLoops`：accept 后按轮询把连接分发给子循环，连接表加互斥锁保护跨线程增删；
- `RpcServer` 新增 `ioThreads` 参数：>0 时自动创建并启动 EventLoopPool（[include/minirpc/rpc/RpcServer.h](../include/minirpc/rpc/RpcServer.h)）。

### 6.2 同机同方法对比（Release，各档总请求 2 万）

| 配置 | 40 并发 QPS | 100 并发 QPS | 200 并发 QPS | 200 并发 P99 |
|---|---:|---:|---:|---:|
| 单 Reactor（io=0, pool=4） | 25311 | 23485 | 22549 | 61.3ms |
| 多 Reactor（io=4, pool=8） | 52763 | 66118 | 69124 | 7.7ms |
| 多 Reactor（io=8, pool=16） | — | 64731 | 78827（长跑 80000 请求 88580） | 6.5ms |

### 6.3 服务端资源（200 并发 × 400 次 = 80000 请求）

| 指标 | 单 Reactor | 多 Reactor（io=8, pool=16） |
|---|---|---|
| CPU | 平均 125.6%（约 1.26 核） | 平均 418.5%（约 4.18 核），峰值 503% |
| 内存 RSS | 峰值 6.6 MB | 峰值 7.0 MB |
| QPS | 约 2.3-2.9 万 | 7.3-8.9 万 |

### 6.4 结论

1. **多核利用率提升约 3.3 倍**：服务端实际占用核数 1.26 → 4.18；
2. **200 并发吞吐提升约 3.5 倍**：2.25 万 → 约 7.9-8.8 万 QPS；
3. **高并发 P99 显著改善**：200 并发 61ms → 6.5ms（事件分发分摊到多个循环，排队大幅下降）；
4. io=8/pool=16 为当前最优配置；400 并发（约 7.8 万 QPS）开始出现客户端侧排队长尾。

说明：单 Reactor 的"提升路径"中"多 Reactor"一项已落地；剩余方向（批量唤醒/合并发送、异步客户端、SO_REUSEPORT 多进程）仍为后续优化。
